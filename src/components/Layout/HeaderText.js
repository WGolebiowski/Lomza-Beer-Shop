import classes from "./HeaderText.module.css";

const HeaderText = () => {
  return (
    <div className={classes.textbox}>
      <h1 className={classes.text}>Zasmakuj w Łomży!</h1>
    </div>
  );
};

export default HeaderText;
